1.メモリから命令を読み込む
2.解釈する
3.実行する
4.結果を保存する


- どうやってメモリから命令を読み込む(最初は手入力)

- レジスタは5bit, 4bitで値を保持、1bitで繰り上がりなどを用いる

0. プログラムがキックされる。helzの値で速度が決まる
1. メモリから命令を読み込む
  - メモリ(配列)をforで回す
  - メモリは2進数
2. 解釈する
  - a ^ b === 0b0 を使って命令を取得
  - 必要とあらばimitiate bitを使う
3. 実行する
  - 実行しろ!
  - JNCとJUMPがある場合、forのiの値を書き換える
  - 加算の場合、x >>> 4 で1の際、繰り上がり
  - OUTが存在する場合、OUTする
4. 結果を保存する
  - 3でOUTがされている場合、保存する